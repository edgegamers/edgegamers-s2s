# Reference API

This private plugin demonstrates how a Source2Script workspace plugin owns a typed runtime contract. It publishes `ReferenceGreetingApi` under the interface name `@edgegamers/reference-api` and supplies the implementation from `src/greeting.ts`.

The plugin is a workspace verification fixture, not a registry package. Keep it until a real producer and consumer replace its sibling-interface coverage.
