# GitHub Repository Setup

Repository files in this directory define local policy, templates, and workflows. GitHub repository state still needs maintainer setup in the web UI or GitHub CLI.

Use [Manual GitHub Setup](./MANUAL_SETUP.md) after the initial branch push. That file lists branch rules, environments, labels, secrets, and release-path stubs that cannot be completed from local files alone.
